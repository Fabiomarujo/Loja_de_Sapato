from django.db import models
from django.utils import timezone

# Create your models here.
# class Contato(models.Model):
#     nome_completo = models.CharField(max_length=60, help_text='Digite seu nome completo')
#     email = models.CharField(help_text='Digite seu e-mail')
#     telefone = models.IntegerField(help_text='Digite o número de telefone')
#     hora_contato = models.CharField(help_text='Selecione a hora para contato')
#     mensagem = models.CharField(help_text='Digite sua mensagem')


# def __str__(self):
#     return f'{self.nome_completo} - {self.email} - {self.telefone} - {self.hora_contato} - {self.mensagem}'
